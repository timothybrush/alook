{
  "name": "@alook/email-worker",
  "version": "0.0.1",
  "private": true,
  "scripts": { "dev": "wrangler dev --persist-to ../web/.wrangler/state", "deploy": "wrangler deploy", "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": { "@alook/shared": "workspace:*", "nanoid": "latest" },
  "devDependencies": { "vitest": "^4.1.3", "wrangler": "latest", "typescript": "latest", "@cloudflare/workers-types": "latest", "@types/node": "latest" }
}
